meta {
  name: Update Note
  type: http
  seq: 9
}

put {
  url: http://localhost:8080/notes/2
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "minha anotacao atualizada joao",
    "content": "deu certo!"
  }
}

settings {
  encodeUrl: true
}
