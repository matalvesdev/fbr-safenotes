meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "joao",
    "password": "senha"
  }
}

vars:post-response {
  token: res.body.accessToken
}

settings {
  encodeUrl: true
}
