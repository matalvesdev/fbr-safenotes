meta {
  name: Profile
  type: http
  seq: 7
}

get {
  url: http://localhost:8080/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "username": "bruno",
    "password": "senha"
  }
}

settings {
  encodeUrl: true
}
