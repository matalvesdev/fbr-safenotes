meta {
  name: Create Note
  type: http
  seq: 8
}

post {
  url: http://localhost:8080/notes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "minha anotacao joao",
    "content": "deu certo!"
  }
}

settings {
  encodeUrl: true
}
