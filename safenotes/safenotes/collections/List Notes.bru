meta {
  name: List Notes
  type: http
  seq: 6
}

get {
  url: http://localhost:8080/notes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "username": "bruno",
    "password": "senha"
  }
}

settings {
  encodeUrl: true
}
